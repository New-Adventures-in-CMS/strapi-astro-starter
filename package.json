{
  "name": "strapi-astro-starter",
  "version": "0.1.0",
  "description": "Production-ready starter kit — Strapi 5 headless CMS + Astro 5 frontend, with dynamic forms, navigation plugin, and Tailwind CSS v4",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently --names CMS,WEB --prefix-colors green,blue \"npm run dev:cms\" \"npm run dev:web\"",
    "dev:cms": "npm --prefix cms run develop",
    "dev:web": "npm --prefix frontend run dev",
    "install:all": "npm --prefix cms install && npm --prefix frontend install",
    "setup": "npm --prefix cms run setup && npm --prefix frontend run setup",
    "build": "npm --prefix cms run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
