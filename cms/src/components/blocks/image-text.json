{
  "collectionName": "components_blocks_image_texts",
  "info": {
    "displayName": "Image + Text",
    "icon": "columns",
    "description": "Image and text side by side, reversible"
  },
  "options": {},
  "attributes": {
    "heading": {
      "type": "string"
    },
    "body": {
      "type": "richtext",
      "required": true
    },
    "image": {
      "type": "media",
      "allowedTypes": ["images"],
      "multiple": false,
      "required": true
    },
    "image_position": {
      "type": "enumeration",
      "enum": ["left", "right"],
      "default": "left"
    }
  }
}
