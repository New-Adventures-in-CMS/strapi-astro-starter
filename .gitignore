# OS
.DS_Store
Thumbs.db

# Secrets — mai committare
.env
.env.local
.env.production

# Dependencies
node_modules/

# Strapi uploads — versiona solo la cartella, non i file caricati
/cms/public/uploads/*
!/cms/public/uploads/.gitkeep

# Local STATUS docs (branch/phase tracking — never ship with clones)
STATUS-*.md

# Process playbook — lives in its own private repo
docs/

# Playwright
playwright-report/
test-results/
